{
  "name": "catpablog",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "install:blog": "cd blog && npm install",
    "generate": "npm run install:blog && cd blog && npm run generate",
    "dev": "cd blog && npm run dev",
    "build": "npm run install:blog && cd blog && npm run build"
  }
}
